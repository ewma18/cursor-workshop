/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: [
    "@nimbus-ds/components",
    "@nimbus-ds/patterns",
    "@nimbus-ds/icons",
  ],
};

export default nextConfig;
